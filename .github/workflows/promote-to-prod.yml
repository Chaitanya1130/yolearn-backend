name: Promote to Prod

on:
  workflow_dispatch:

jobs:
  push-to-prod:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout test repo
      uses: actions/checkout@v3

    - name: Push agent1 to prod repo
      uses: ad-m/github-push-action@v0.6.0
      with:
        github_token: ${{ secrets.PROD_PUSH_TOKEN }}  # ✅ This is the correct input key
        repository: Chaitanya1130/yolearn-backend
        branch: main
        directory: student/orch1/agents/agent1
        force: true
